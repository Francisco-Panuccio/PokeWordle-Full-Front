import { CertificateDirective } from './certificate.directive';

describe('CertificateDirective', () => {
  it('should create an instance', () => {
    const directive = new CertificateDirective();
    expect(directive).toBeTruthy();
  });
});
