import { BackgroundRegionsDirective } from './background-regions.directive';

describe('BackgroundRegionsDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundRegionsDirective();
    expect(directive).toBeTruthy();
  });
});
