import { PkmnTypeDirective } from './pkmn-type.directive';

describe('PkmnTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new PkmnTypeDirective();
    expect(directive).toBeTruthy();
  });
});
